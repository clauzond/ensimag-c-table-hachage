# TP Dictionnaire (Damien C.)

Pour compiler
```
source ./lib_path.sh (ou "export LD_LIBRARY_PATH=$PWD/lib")
make clean
make
```

Pour exécuter les programmes de test :
```
./tests/directory-0X-test
```